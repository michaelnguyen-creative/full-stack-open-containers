version: '3.8'

services:
  todo-frontend:
    image: fso12/todo-frontend-dev
    build:
      context: .
      dockerfile: dev.Dockerfile
    container_name: fso_todo_frontend_dev
    volumes:
      - ./:/usr/src/app
    ports:
      - 3000:3000


